{
  "name": "@premise/server",
  "version": "0.2.0-rc.1",
  "description": "Small HTTP/JSON API for PREMiSE v2 runtime, hybrid search, and context packing.",
  "type": "module",
  "dependencies": {
    "@premise/context-engine": "workspace:*",
    "@premise/index-hybrid": "workspace:*",
    "@premise/protocol-types": "workspace:*",
    "@premise/runtime-core": "workspace:*"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm build && node test/server.test.mjs"
  }
}
