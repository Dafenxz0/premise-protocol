{
  "name": "@premise/runtime-core",
  "version": "0.2.0-rc.1",
  "description": "PREMiSE v2 runtime with evidence, dependency propagation, tenancy, validation, and replayable events.",
  "type": "module",
  "dependencies": {
    "@premise/protocol-types": "workspace:*"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm build && node test/runtime.test.mjs"
  }
}
