# PREMiSE v2 benchmark dashboard

Open `index.html` from a static server rooted at the repository to see the latest `benchmarks/real-world-v2/results.json`. The page has a built-in offline fallback so the table remains useful when opened directly. It is a static artifact, not a hosted cloud dashboard.
